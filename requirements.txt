# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
# --- Gemini (AI Studio o Vertex AI) ---
google-genai>=1.0
# --- Deforestación (Google Earth Engine) ---
earthengine-api>=1.0
# --- Geo + Dossier ---
shapely>=2.0
rasterio>=1.3
reportlab>=4.0
requests>=2.31
staticmap>=0.5
qrcode>=7.0
# --- Google Cloud (opcional: Firestore + Storage) ---
# Acotado a la serie 2.x/5.x conocida: un salto de versión mayor en un build
# rompió Firestore en silencio (25-ago-2026) y se perdieron datos de un día.
google-cloud-firestore>=2.16,<3
google-cloud-storage>=2.16,<3
protobuf>=4.25,<6
# --- Auth (Login con Google) ---
google-auth>=2.28,<3
itsdangerous>=2.1
# --- Notarización Fase 1: anclaje en Bitcoin (OpenTimestamps) ---
opentimestamps-client>=0.7
# --- Importación masiva (Excel) ---
openpyxl>=3.1
